/* 
?  


*/

// ? reloads page every 2 sec
// setTimeout(e=>{window.location.reload()},5000)

/* 
................................................
class Comment{
    constructor({id,username,comment}){
        this.id=id;
        this.username=username;
        this.comment=comment;
    }
    singleCommentElem(comment=this) { return `<li id="${comment.id}" class="comment">${(comment.username)} : ${comment.comment}</li>`; }
} 
................................................
*/